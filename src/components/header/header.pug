include ../logo/logo
include ../nav/nav

mixin header({logoImg, menuList})
  header.header
    .header__container.container
      +logo({src: logoImg})(class = 'header__logo')
      +nav({list: menuList})(class = 'header__nav')
